<h1><%= 'Restaurants'%></h1>


<% @restaurants.each do |t|  %>
  <div class="flexbox">
    <li>
    <h5><%= link_to t.name, restaurant_path(t) %></h5>
  </li>
  </div>

<% end %>

  <h4><%= link_to "+ Add new Restaurant", new_restaurant_path %><h4>
