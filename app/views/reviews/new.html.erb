<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit 'Create me' %>
<% end %>

<%= link_to "Show all Restaurants", restaurants_path %>
